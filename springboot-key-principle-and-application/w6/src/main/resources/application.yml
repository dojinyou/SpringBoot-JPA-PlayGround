management:
  server:
    port: 8081
  info:
#    java:
#      enabled: true
#    os:
#      enabled: true
#    env:
#      enabled: true
#    build:
#      enabled: true
    git:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      # show-details: always
      show-components: always
info:
  app:
    name: hello-actuator

logging:
  level:
    com.dojinyou.inflearn.roadmap.w6: debug
